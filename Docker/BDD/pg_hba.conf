# =============================================================================
# pg_hba.conf — Nœuds Workers Citus
# Les workers n'autorisent les connexions que depuis le coordinator.
# =============================================================================

local   all             all                                     trust

# Connexions depuis le coordinator Citus et entre workers
host    all             all             172.16.0.0/12           scram-sha-256
host    all             all             10.0.0.0/8              scram-sha-256
host    all             all             127.0.0.1/32            scram-sha-256
